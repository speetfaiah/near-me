//import Vue from 'vue'

export default {
    increment(state) {
        state.count++
    }
}

/*function createThread(state, id, name) {
    Vue.set(state.threads, id, {
        id,
        name,
        messages: [],
        lastMessage: null
    })
}*/